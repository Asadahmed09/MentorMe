{
  "name": "mentorme-platform",
  "version": "1.0.0",
  "description": "Student Mentorship Platform - Connect with top-performing mentors (MVP)",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "preview": "npm run preview --prefix client",
    "install:all": "npm install --prefix client"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
